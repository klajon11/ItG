N = [0.85 0 0 0.85 0.8 0 0 0.8 0.2 0.15 -0.08 0.22 -0.2 0.15 0.08 0.22 0.25 0.12 -0.1 0.25 -0.2 0.12 0.1 0.2 0.03 0 0 0.1];
M = reshape(N,2,2,7);
a = [0 1.5 0 1.5 0 0.85 0 0.85 0 0.3 0 0.4 0 0];
b = reshape(a,2,1,7);
v = transpose([0 0]);
X = zeros(25000,2);
pi = [0.6 0.1 0.07 0.07 0.07 0.07 0.02];
pk = cumsum(pi);

for i = 1:25000
    j = find(rand()<pk,1);
    v = M(:,:,j) * v + b(:,:,j);
    X(i,:) = v;
end

scatter(X(:,1),X(:,2),'.', 'SizeData',8);
